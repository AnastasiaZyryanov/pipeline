[project]
name = "pipeline"
version = "0.1.0"
description = "Pipeline library"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jsonschema>=4.26.0",
    "pydantic>=2.12.5",
    "requests>=2.33.0",
]
[tool.uv]
package = true